export default class PayloadWidget {

    constructor(container) {

        this.container = container;

    }

    render(query) {
/*
        const payload = query; // {query};
 
        this.container.textContent =
            JSON.stringify(payload, null, 2);
*/
    }

}